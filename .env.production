# Production environment variables for deployment
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Production API URL - Amplify will provide this automatically
# The API endpoint will be available at /api through AWS API Gateway
NEXT_PUBLIC_API_URL=/api
PORT=3000
HOSTNAME=0.0.0.0
# APPLICATIONINSIGHTS_CONNECTION_STRING will be injected by Azure

# Custom environment variables (add your own here)
# CUSTOM_KEY=your-value-here