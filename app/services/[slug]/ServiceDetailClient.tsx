// ServiceDetailClient has been removed as individual service pages are no longer used.
// All services are now displayed on the main /services page as a catalogue.

export default function ServiceDetailClient() {
  return null;
}
