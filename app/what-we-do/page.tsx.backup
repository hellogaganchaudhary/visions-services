'use client';

import { motion } from 'framer-motion';
import { useInView } from 'react-intersection-observer';
import {
  Target, Lightbulb, Rocket, Users, TrendingUp, Award,
  Code2, Cpu, Globe, Shield, CheckCircle, Zap, BarChart3, Clock
} from 'lucide-react';
import StatsCounter from '@/components/StatsCounter';

export default function WhatWeDoPage() {
  const [ref, inView] = useInView({ triggerOnce: true, threshold: 0.1 });

  const approach = [
    {
      icon: Lightbulb,
      title: 'Discovery & Planning',
      description: 'We dive deep into your business goals, target audience, and competitive landscape to create a comprehensive strategy.',
      color: '#eab308',
      duration: '1-2 weeks',
    },
    {
      icon: Code2,
      title: 'Design & Development',
      description: 'Our expert team brings your vision to life with cutting-edge technologies and beautiful, intuitive designs.',
      color: '#3b82f6',
      duration: '4-8 weeks',
    },
    {
      icon: Rocket,
      title: 'Testing & Launch',
      description: 'Rigorous quality assurance ensures flawless performance before we launch your project to the world.',
      color: '#22c55e',
      duration: '1-2 weeks',
    },
    {
      icon: TrendingUp,
      title: 'Growth & Optimization',
      description: 'Continuous monitoring and optimization to ensure your digital products keep performing at their best.',
      color: '#a855f7',
      duration: 'Ongoing',
    },
  ];

  const expertise = [
    {
      icon: Globe,
      title: 'Full-Stack Development',
      description: 'End-to-end development expertise across frontend, backend, and database technologies.',
      color: '#06b6d4',
      technologies: ['React', 'Node.js', 'Python', 'PostgreSQL'],
    },
    {
      icon: Cpu,
      title: 'Cloud & DevOps',
      description: 'Scalable cloud infrastructure and automated deployment pipelines for maximum efficiency.',
      color: '#f97316',
      technologies: ['AWS', 'Azure', 'Docker', 'Kubernetes'],
    },
    {
      icon: Shield,
      title: 'Security First',
      description: 'Enterprise-grade security measures to protect your data and users.',
      color: '#ef4444',
      technologies: ['SSL/TLS', 'OAuth', 'Encryption', 'Compliance'],
    },
    {
      icon: Users,
      title: 'User-Centric Design',
      description: 'Designs that prioritize user experience and drive meaningful engagement.',
      color: '#ec4899',
      technologies: ['Figma', 'Adobe XD', 'User Testing', 'Analytics'],
    },
  ];

  const stats = [
    {
      value: 500,
      suffix: '+',
      label: 'Projects Completed',
      icon: <CheckCircle className="w-8 h-8" />,
      color: '#3b82f6',
    },
    {
      value: 200,
      suffix: '+',
      label: 'Happy Clients',
      icon: <Users className="w-8 h-8" />,
      color: '#22c55e',
    },
    {
      value: 98,
      suffix: '%',
      label: 'Satisfaction Rate',
      icon: <Award className="w-8 h-8" />,
      color: '#eab308',
    },
    {
      value: 5,
      suffix: '+',
      label: 'Years Experience',
      icon: <TrendingUp className="w-8 h-8" />,
      color: '#a855f7',
    },
  ];

  const caseStudies = [
    {
      title: 'E-Commerce Platform',
      client: 'RetailCorp',
      result: '300% increase in sales',
      color: '#3b82f6',
      icon: <BarChart3 className="w-6 h-6" />,
    },
    {
      title: 'Mobile Banking App',
      client: 'FinTech Solutions',
      result: '1M+ downloads',
      color: '#22c55e',
      icon: <Users className="w-6 h-6" />,
    },
    {
      title: 'Cloud Migration',
      client: 'Enterprise Inc',
      result: '60% cost reduction',
      color: '#f97316',
      icon: <TrendingUp className="w-6 h-6" />,
    },
  ];

  return (
    <div className="min-h-screen pt-32 pb-24">
      {/* Hero Section */}
      <section className="px-4 sm:px-6 lg:px-8 mb-24">
        <div className="max-w-7xl mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            className="text-center mb-16"
          >
            <h1 className="text-5xl sm:text-6xl lg:text-7xl font-bold mb-6">
              <span className="gradient-text">What We Do</span>
            </h1>
            <p className="text-xl text-gray-400 max-w-3xl mx-auto">
              We transform ideas into powerful digital experiences that drive real business results
            </p>
          </motion.div>

          {/* Mission Statement */}
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.2 }}
            className="glass rounded-3xl p-12 max-w-5xl mx-auto text-center"
          >
            <Target className="w-16 h-16 text-primary-400 mx-auto mb-6" />
            <h2 className="text-3xl font-bold mb-4 text-white">Our Mission</h2>
            <p className="text-xl text-gray-300 leading-relaxed">
              We empower businesses to thrive in the digital age by delivering innovative software solutions 
              that combine cutting-edge technology with exceptional user experiences. Our comprehensive approach 
              covers everything from custom web development to cloud optimization, ensuring your business has 
              the tools it needs to succeed.
            </p>
          </motion.div>
        </div>
      </section>

      {/* Our Approach */}
      <section ref={ref} className="px-4 sm:px-6 lg:px-8 mb-24">
        <div className="max-w-7xl mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            animate={inView ? { opacity: 1, y: 0 } : {}}
            className="text-center mb-16"
          >
            <h2 className="text-4xl sm:text-5xl font-bold mb-4">
              <span className="text-white">Our </span>
              <span className="gradient-text">Approach</span>
            </h2>
            <p className="text-xl text-gray-400 max-w-2xl mx-auto">
              A proven methodology that delivers results every time
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6" style={{ perspective: '1000px' }}>
            {approach.map((step, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, y: 50 }}
                animate={inView ? { opacity: 1, y: 0 } : {}}
                transition={{ delay: index * 0.15, duration: 0.6 }}
                whileHover={{ 
                  y: -15,
                  rotateY: 5,
                  transition: { duration: 0.3 }
                }}
                className="relative group"
                style={{ transformStyle: 'preserve-3d' }}
              >
                {/* Connecting line */}
                {index < approach.length - 1 && (
                  <motion.div 
                    className="hidden lg:block absolute top-1/2 -right-3 transform -translate-y-1/2 z-10"
                    initial={{ scaleX: 0 }}
                    animate={inView ? { scaleX: 1 } : {}}
                    transition={{ delay: index * 0.15 + 0.5, duration: 0.5 }}
                  >
                    <div className="w-6 h-1 rounded-full" style={{ background: step.color }}></div>
                  </motion.div>
                )}

                {/* Glow effect */}
                <div 
                  className="absolute -inset-1 rounded-2xl blur-xl opacity-0 group-hover:opacity-75 transition-opacity duration-500"
                  style={{ background: `${step.color}60` }}
                ></div>

                {/* Card */}
                <div 
                  className="relative rounded-2xl p-8 h-full border transition-all duration-500"
                  style={{
                    background: 'rgba(15, 15, 35, 0.6)',
                    backdropFilter: 'blur(20px)',
                    borderColor: 'rgba(255, 255, 255, 0.1)',
                  }}
                >
                  <motion.div
                    whileHover={{ rotate: 360, scale: 1.1 }}
                    transition={{ duration: 0.6 }}
                    className="p-4 rounded-xl w-fit mb-4"
                    style={{ 
                      background: step.color,
                      boxShadow: `0 10px 40px -10px ${step.color}80`,
                    }}
                  >
                    <step.icon className="w-8 h-8 text-white" />
                  </motion.div>
                  
                  <div className="flex items-center justify-between mb-2">
                    <div className="font-bold text-lg" style={{ color: step.color }}>
                      Step {index + 1}
                    </div>
                    <div className="flex items-center space-x-1 text-sm text-gray-400">
                      <Clock className="w-4 h-4" />
                      <span>{step.duration}</span>
                    </div>
                  </div>
                  
                  <h3 
                    className="text-2xl font-bold mb-3"
                    style={{
                      background: `linear-gradient(135deg, #ffffff, ${step.color})`,
                      WebkitBackgroundClip: 'text',
                      WebkitTextFillColor: 'transparent',
                      backgroundClip: 'text',
                    }}
                  >
                    {step.title}
                  </h3>
                  <p className="text-gray-400 leading-relaxed">{step.description}</p>

                  {/* Number decoration */}
                  <div 
                    className="absolute -top-4 -right-4 w-12 h-12 rounded-full flex items-center justify-center font-bold text-2xl opacity-20 group-hover:opacity-40 transition-opacity"
                    style={{ background: step.color }}
                  >
                    {index + 1}
                  </div>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Stats Section */}
      <section className="px-4 sm:px-6 lg:px-8 mb-24">
        <div className="max-w-7xl mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            className="text-center mb-12"
          >
            <h2 className="text-4xl sm:text-5xl font-bold mb-4">
              <span className="text-white">Our </span>
              <span className="gradient-text">Impact</span>
            </h2>
            <p className="text-xl text-gray-400">Numbers that speak for themselves</p>
          </motion.div>
          
          <StatsCounter stats={stats} />
        </div>
      </section>

      {/* Expertise Areas */}
      <section className="px-4 sm:px-6 lg:px-8 mb-24">
        <div className="max-w-7xl mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl sm:text-5xl font-bold mb-4">
              <span className="text-white">Our </span>
              <span className="gradient-text">Expertise</span>
            </h2>
            <p className="text-xl text-gray-400 max-w-2xl mx-auto">
              Deep technical knowledge across the full technology stack
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-6" style={{ perspective: '1000px' }}>
            {expertise.map((item, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, y: 50 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: index * 0.1, duration: 0.6 }}
                whileHover={{ 
                  y: -10,
                  scale: 1.02,
                  rotateX: 5,
                  transition: { duration: 0.3 }
                }}
                className="relative group"
                style={{ transformStyle: 'preserve-3d' }}
              >
                {/* Glow effect */}
                <div 
                  className="absolute -inset-1 rounded-3xl blur-2xl opacity-0 group-hover:opacity-75 transition-opacity duration-500"
                  style={{ background: `${item.color}60` }}
                ></div>

                {/* Card */}
                <div 
                  className="relative rounded-3xl p-8 border transition-all duration-500"
                  style={{
                    background: 'rgba(15, 15, 35, 0.6)',
                    backdropFilter: 'blur(20px)',
                    borderColor: 'rgba(255, 255, 255, 0.1)',
                  }}
                >
                  <motion.div 
                    className="absolute inset-0 opacity-0 group-hover:opacity-20 rounded-3xl transition-opacity duration-500"
                    style={{ 
                      background: `radial-gradient(circle at top left, ${item.color}60, transparent 70%)`,
                    }}
                  ></motion.div>

                  <div className="relative z-10">
                    <motion.div
                      whileHover={{ rotate: 360, scale: 1.1 }}
                      transition={{ duration: 0.6 }}
                      className="p-4 rounded-xl w-fit mb-4"
                      style={{ 
                        background: item.color,
                        boxShadow: `0 10px 40px -10px ${item.color}80`,
                      }}
                    >
                      <item.icon className="w-8 h-8 text-white" />
                    </motion.div>
                    
                    <h3 
                      className="text-3xl font-bold mb-3"
                      style={{
                        background: `linear-gradient(135deg, #ffffff, ${item.color})`,
                        WebkitBackgroundClip: 'text',
                        WebkitTextFillColor: 'transparent',
                        backgroundClip: 'text',
                      }}
                    >
                      {item.title}
                    </h3>
                    <p className="text-gray-400 mb-6 leading-relaxed">{item.description}</p>

                    {/* Technology tags */}
                    <div className="flex flex-wrap gap-2">
                      {item.technologies.map((tech, idx) => (
                        <motion.span
                          key={idx}
                          whileHover={{ scale: 1.1, y: -2 }}
                          className="px-3 py-1 rounded-full text-sm font-medium border"
                          style={{
                            background: `${item.color}20`,
                            borderColor: `${item.color}40`,
                            color: item.color,
                          }}
                        >
                          {tech}
                        </motion.span>
                      ))}
                    </div>
                  </div>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Case Studies Preview */}
      <section className="px-4 sm:px-6 lg:px-8 mb-24">
        <div className="max-w-7xl mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            className="text-center mb-12"
          >
            <h2 className="text-4xl sm:text-5xl font-bold mb-4">
              <span className="text-white">Success </span>
              <span className="gradient-text">Stories</span>
            </h2>
            <p className="text-xl text-gray-400">Real results for real businesses</p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            {caseStudies.map((study, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: index * 0.1 }}
                whileHover={{ y: -10, scale: 1.05 }}
                className="relative group"
              >
                <div 
                  className="absolute -inset-1 rounded-2xl blur-xl opacity-0 group-hover:opacity-75 transition-opacity duration-500"
                  style={{ background: `${study.color}60` }}
                ></div>

                <div 
                  className="relative glass rounded-2xl p-6 border border-white/10 text-center"
                  style={{
                    background: 'rgba(15, 15, 35, 0.8)',
                    backdropFilter: 'blur(20px)',
                  }}
                >
                  <div 
                    className="p-4 rounded-xl w-fit mx-auto mb-4"
                    style={{ background: study.color }}
                  >
                    {study.icon}
                  </div>
                  <h3 className="text-xl font-bold text-white mb-2">{study.title}</h3>
                  <p className="text-gray-400 text-sm mb-4">{study.client}</p>
                  <div 
                    className="text-2xl font-bold"
                    style={{ color: study.color }}
                  >
                    {study.result}
                  </div>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>
        <div className="max-w-7xl mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl sm:text-5xl font-bold mb-4">
              <span className="text-white">Our </span>
              <span className="gradient-text">Expertise</span>
            </h2>
            <p className="text-xl text-gray-400 max-w-2xl mx-auto">
              Deep technical knowledge across multiple domains
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
            {expertise.map((item, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, x: index % 2 === 0 ? -30 : 30 }}
                whileInView={{ opacity: 1, x: 0 }}
                viewport={{ once: true }}
                transition={{ delay: index * 0.1, duration: 0.6 }}
                whileHover={{ scale: 1.02 }}
                className="glass rounded-2xl p-8 hover:border-primary-500/50 transition-all"
              >
                <item.icon className="w-12 h-12 text-primary-400 mb-4" />
                <h3 className="text-2xl font-bold text-white mb-3">{item.title}</h3>
                <p className="text-gray-400 text-lg">{item.description}</p>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Stats Section */}
      <section className="px-4 sm:px-6 lg:px-8">
        <div className="max-w-7xl mx-auto">
          <motion.div
            initial={{ opacity: 0, scale: 0.95 }}
            whileInView={{ opacity: 1, scale: 1 }}
            viewport={{ once: true }}
            className="glass rounded-3xl p-12 border-2 border-primary-500/30"
          >
            <div className="grid grid-cols-2 md:grid-cols-4 gap-8">
              {[
                { value: '500+', label: 'Projects Completed' },
                { value: '200+', label: 'Happy Clients' },
                { value: '50+', label: 'Team Members' },
                { value: '15+', label: 'Countries Served' },
              ].map((stat, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.1 }}
                  className="text-center"
                >
                  <div className="text-5xl font-bold gradient-text mb-2">{stat.value}</div>
                  <div className="text-gray-400">{stat.label}</div>
                </motion.div>
              ))}
            </div>
          </motion.div>
        </div>
      </section>
    </div>
  );
}
